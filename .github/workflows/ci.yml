on: push

jobs:
  build:
    runs-on: ubuntu-latest
    name: build
    steps:
        - uses: actions/checkout@v5
        - run: |
            cd frontend
            npm install
            npm run build
            npx prettier src/ -c
